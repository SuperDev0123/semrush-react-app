import { makeStyles } from '@material-ui/core'

const useTestDetailsBlockStyles = makeStyles((theme) => ({
  headerTitle: {
    fontFamily: "'Poppins'",
    fontStyle: 'normal',
    fontWeight: 600,
    fontSize: '14px',
    lineHeight: '21px',
    color: '#19519D',
    marginBottom: '11px',
  },
}))

export default useTestDetailsBlockStyles
