import { makeStyles } from '@material-ui/core'

const useAddCriteriaButtonStyles = makeStyles((theme) => ({
  root: {
    width: 'fit-content',
    fontFamily: "'Poppins'",
    fontStyle: 'normal',
    fontWeight: 400,
    fontSize: '13px',
    lineHeight: '18px',
    textAlign: 'right',
    color: '#016AE9',
    cursor: 'pointer',
    marginTop: '27px',
    marginLeft: 'auto',
  },
}))

export default useAddCriteriaButtonStyles
