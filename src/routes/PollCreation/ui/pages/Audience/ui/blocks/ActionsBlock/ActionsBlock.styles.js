import { makeStyles } from '@material-ui/core'

const useActionsBlockStyles = makeStyles((theme) => ({
  actionsBlock: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
}))

export default useActionsBlockStyles
